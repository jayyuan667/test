# 任务计划：Linux 服务器生产上线

## 目标
将智能二维工艺系统从当前"试跑通过基础环境"状态推进到**可验收的生产就绪**状态，完成代码交付、业务链路验收和服务生产化。

## 当前阶段
阶段 4

## 关键路径节点（Critical Path）

```
┌─────────────────────────────────────────────────────────────────┐
│  CP1: 代码冻结      CP2: 服务器部署    CP3: 业务验收    CP4: 生产化 │
│  [本地]              [服务器]            [端到端]          [运维]   │
│                                                                    │
│  提交→打tar→校验    停服→换码→重启    5项必做验收      systemd+Nginx │
│      ↓                  ↓                 ↓                ↓      │
│  ⚠️ 阻断点:          ⚠️ 阻断点:         ⚠️ 阻断点:       可选     │
│  任何未提交改动      旧进程占用端口     LLM key未配      磁盘93%    │
│  阻止打纯净包        .env被覆盖        YOLO缺失不阻断   需先清空间  │
└─────────────────────────────────────────────────────────────────┘
```

## 各阶段

### 阶段 1：代码冻结与打包（本地 Mac）
- [x] 1.1 确认所有关键修复已提交
- [x] 1.2 排除不提交的文件（backups/, .env, node_modules/ 等）
- [x] 1.3 生成纯净源码 tar 包
- [x] 1.4 本地验证 tar 包完整性（解压→检查关键文件存在）
- **状态：** complete
- **产物：** `/tmp/smart-process-system-20260621_230302.tar.gz` (1.2M, 309 files)
- **提交链：** 56860e7 → d88203c → bc2e738 → 9e7055c

### 阶段 2：服务器代码更新（Linux 服务器）
- [x] 2.1 检查服务器当前状态（disk 99%⚠️, 5190 运行中）
- [x] 2.2 备份当前运行代码和 .env（`test.backup.20260621_231222`）
- [x] 2.3 上传新 tar 包到服务器
- [x] 2.4 停止旧后端进程（pkill）
- [x] 2.5 替换代码目录（tar解压 + .env恢复）
- [x] 2.6 运行 deploy_check.sh 自检（uv需bash -l, node需nvm, npm ci后通过）
- [x] 2.7 重新构建前端（含 copyright 更新）
- [x] 2.8 重启后端（PID 2940161）
- [x] 2.9 验证 health 200 + capabilities（vision_api: true, doubao）
- **状态：** complete
- **⚠️ 遗留问题：** 磁盘 99%，后续阶段需清理

### 阶段 3：.env 配置验证
- [x] 3.1 LLM_API_KEY/LLM_BASE_URL/LLM_MODEL = deepseek configured
- [x] 3.2 EMBEDDING_API_KEY/EMBEDDING_BASE_URL/EMBEDDING_MODEL configured
- [x] 3.3 VISION_* = doubao configured
- **状态：** complete
- **结论：** 所有 key 已配置，与旧 .env 一致

### 阶段 4：业务链路验收（5 项必做）
- [ ] 4.1 sample ZIP 导入 → 不报无效 PDF
- [ ] 4.2 真实 PDF 上传 → 跳过 YOLO → 特征审阅 → LLM 工艺生成
- [ ] 4.3 工艺入库 → 知识库浏览页能看到记录（验证 sessionStorage 解锁）
- [ ] 4.4 删除库记录 → 不报 drawing_features 表缺失
- [ ] 4.5 端口转发 → Mac 浏览器能完整走通全流程
- **完成标准：** 5 项全部通过，server.log 无未预期错误
- **回退方案：** 逐项排查，单项失败不阻塞其他项

### 阶段 5：生产化（可选，视时间决定）
- [ ] 5.1 清理磁盘空间（日志、缓存、旧备份）
- [ ] 5.2 安装 systemd 服务文件
- [ ] 5.3 配置 Nginx 反向代理
- [ ] 5.4 放置 YOLO 模型文件（如果可用）
- [ ] 5.5 配置日志轮转
- **完成标准：** 服务开机自启，Nginx 代理生效
- **回退方案：** systemd disable + 恢复 nohup 启动

## 架构兼容性检查点

| 检查项 | 风险 | 缓解措施 |
|--------|------|----------|
| Python 版本漂移 | 服务器 Python 3.12.3 vs 项目要求 3.11.15 | uv 会固定 3.11.15，deploy_check 已验证通过 |
| Node 版本 | 需 v20+ | deploy_check 已确认 nvm 切换到 20 |
| SQLite 向后兼容 | 新代码可能依赖新表/新字段 | 部署前备份 db 文件，启动时自动建表 |
| .env 格式 | 新代码可能新增配置项 | 先检查旧 .env 再合并新配置 |
| 前端构建产物 | 新旧 API 路径不一致 | 每次部署重新 build |
| 端口占用 | 旧进程未完全退出 | 停服后 sleep 3 + ss 检查 |
| 磁盘空间 | 93% 使用率 | 部署前先清理，tar 包不包含大文件 |

## 已做决策
| 决策 | 理由 |
|------|------|
| 先不迁移数据库到 PostgreSQL | Linux 试跑阶段 SQLite 够用，报告明确说"不建议优先投入" |
| 不上 systemd/Nginx 直到业务验收通过 | 试跑报告建议"完成业务链路验收后再考虑" |
| YOLO 模型缺失不阻塞 | 通过"跳过 YOLO 审阅"继续流程 |
| 每次部署保留上一版本备份 | 确保可快速回退 |

## 回退触发条件
- deploy_check.sh 不通过 → 不回退，先排查
- /api/health 非 200 → 检查日志，2次重试后回退
- 业务验收任何一项连续失败 2 次 → 回退并记录错误
- 磁盘满导致服务异常 → 立即清理或回退

## 备注
- 参考文档：`docs/reports/2026-06-21-linux-server-trial-run-handoff.md`
- 服务器访问链路：Mac → 182.151.23.45 → ssh -p 12350 → liu4th
- 代码路径：`~/smart-process-test/test`
- 后端端口：5190
- 前端开发端口（Mac）：3200
