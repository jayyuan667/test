# FORGE v1 真实纵向验收

## 结果

- 时间：2026-07-15 17:24–17:26 CST
- 图纸：`/Users/caojiayuan/Documents/测试包/drawing/test.png`
- 任务 ID：`ac5e7f66-e6e2-40a8-ac0f-4d809bc32aa4`
- 最终状态：`completed / done / 100%`，revision `3019`
- Playwright：1 passed，1.9m
- 真实数据：25 个特征，10 个结构化工艺工序，58 个已解析净化事件
- 恢复：刷新后从 `forge-v1-active-task` 恢复同一 task ID，工艺行仍可见
- 导出：`Process_ac5e7f66.pdf`，`application/pdf`，PDF 1.4，1 页，文件头 `%PDF`
- 预览：通过 workflow client Bearer 认证加载并转为 object URL，E2E 确认图像可见及来源链接可用。

## 真实字段与空值

- 25/25 个特征的 `confidence` 为 `null`，UI 显示“未提供”，未生成伪置信度。
- 特征来源为 `legacy_text`；`source.page` 为 `null`，UI 显示“页码未提供”。
- 工艺行的缺失 `equipment` 以空数组保留，`duration_minutes` 为 `null`，UI 显示“未提供”。
- 快照原始 `task.error` 为空字符串；任务完成且没有失败阶段。

## 证据

证据只位于 `/tmp/forge-v1-evidence/`，不纳入 Git：

- `final-snapshot.json`：最终 v1 快照
- `events-sanitized.json`：58 个已解析事件，递归移除 token/URL/path 字段
- `01-real-phase.png`、`02-structured-operation.png`、`03-resumed-complete.png`
- `Process_ac5e7f66.pdf`
- `task-id.txt`、`browser-diagnostics.txt`

## 命令与门禁

- `.venv/bin/python -m pytest backend/test_task_contract_v1.py backend/test_task_events_v1.py backend/test_tasks_api_v1.py backend/test_sse_events.py backend/test_annotation_finalize_restore.py backend/test_upload_capability_gate.py backend/test_data_isolation.py -q`
  - 78 passed，exit 0。已知 data-isolation 用例的后台 PRT thread 产生 1 个 pytest warning，不影响用例结果。
- `npm run test:workflow`
  - 35 passed，exit 0。Node 仅报现有 module-type 提示。
- focused ESLint over Task 6 frontend/workflow/E2E files
  - exit 0，无 error/warning。
- `npm run build`
  - exit 0，Next.js production build 和静态页生成完成。
- `PW_TEST_HTML_REPORT_OPEN=never npx playwright test --config=e2e/playwright.prod.config.ts e2e/v1-workflow.spec.ts --reporter=line`
  - 1 passed (1.9m)，exit 0。无 mock network，无合成 PDF。
- `curl -fsS http://localhost:5390/api/health`
  - `status=ok`，exit 0。
- `.venv/bin/python -m pytest backend/test_runtime_smoke.py backend/test_sse_events.py -q`
  - Task 6 不再吸收缺失的用户 `scripts.runtime_smoke` 实现；因此该组合命令当前在 collection 阶段因 `ImportError: cannot import name 'runtime_smoke' from 'scripts'` 受限。`backend/test_sse_events.py` 可独立通过，健康接口为 OK。

## Legacy fallback provenance

- `LegacyGeneratePage.tsx` 来自 Task 6 开始时未跟踪的 `GeneratePage.tsx`；原始 SHA-256 为 `434c7de309b3261e2b6c1837e3fa253146613ed1395a83879509d6a86f274bd2`，基线副本保存在 `/tmp/forge-task6-baseline/GeneratePage.tsx.baseline`。
- 该文件是受 `NEXT_PUBLIC_USE_WORKFLOW_V1=false` 保护的不可变过渡 fallback；吸收后 SHA-256 为 `69feb74549796fc0a325198e5c66f9460a4ef81fadccd4eb5fc7df48fa4a45a7`，Task 6 rejection 修复期间未再修改其代码。
- 删除条件：v1 在生产观测窗口内持续通过真实上传、审阅、恢复和导出，且回滚开关经变更批准正式移除后，才可删除 fallback。

## Extra backend changes

- `backend/api/v1/tasks.py`：将持久事件 seq 用作真实 revision，并从 `annotation_required` 恢复预览资产，使 reducer 能保持严格单调且刷新可恢复。
- `backend/services/task_snapshot.py`：仅兼容真实已持久化字段 `process_flow.data` 和 `preview_image_urls`，不改模型或工艺语义。
- `backend/test_upload_capability_gate.py`：用临时 auth/task DB 并断言前后 task ID 集合不变，消除联合门禁对真实测试状态的污染。

## RED/GREEN 缺口记录

- controller 流程 RED：`controller.upload is not a function`；补齐 controller 命令后 GREEN。
- 事件驱动快照 RED：`processing != awaiting_annotation`；controller 在 legacy transition event 后恢复快照。
- revision RED：去除修复后明确失败 `assert 0 > 0`；用持久事件最大 seq 作 revision 后 GREEN。
- 浏览器 SSE RED：`Window.fetch: Illegal invocation`；解绑 fetch receiver 后 GREEN。
- 真实工艺 RED：`process_flow.data` 快照映射为空；adapter 兼容真实字段后 GREEN。
- XLSX 导出在当前运行环境因缺少 pandas 返回 500；使用无该依赖且真实可用的 PDF 导出通过验收。
