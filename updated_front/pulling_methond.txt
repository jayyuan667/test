调用的时候是function handleClickExport() {
  const promptData = preparePromptData();

  exportFileModal.value?.open({
    fileName: generationModel.modelName,
    modelName: generationModel.modelName,
    imageUrl: isModelFile.value ? '' : (imgSrc.value || ''),
    promptData: promptData,
    tableData: tableData.value.map(r => ({ ...r })),
    featureList: featureList,
  });
}